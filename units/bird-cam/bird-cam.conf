
rtmp {
    server {
	listen 1935;
	ping 30s;
	notify_method get;

	application live {
	    live on;

	    allow publish all;
	    allow play all;

	    hls on;
	    hls_path /tmp/live-stream-hls;
	    hls_fragment 15s;
	}

	application vod {
	    play /var/www/vod;
	}
    }
}
